<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Webhook.Service</RootNamespace>
    <AssemblyName>Webhook.Service</AssemblyName>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Webhook.Service.Tests" />
  </ItemGroup>

  <ItemGroup>
    <Content Update="webhooks.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
